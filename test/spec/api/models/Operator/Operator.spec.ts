import {Operator} from '../../../../../src/api/models/Operator/Operator';

describe('Operator', () => {
  let operator:Operator;

  beforeEach(() => {
    operator = new Operator();
  });

  describe('', () => {
    it('', () => {
      // given

      // when

      // then
    });
  });
});
